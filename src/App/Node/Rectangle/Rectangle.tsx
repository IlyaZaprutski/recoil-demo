import React from 'react';
import { DraggableCore, DraggableEvent } from 'react-draggable';

// import './Canvas.scss';

const Element: React.FC = () => {
  const t = 5;

  return <div className="canvasElement">{JSON.stringify(1)}</div>;
};

export default Element;
